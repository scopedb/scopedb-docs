---
interface MenuItem {
  text: string;
  href: string;
  children?: MenuItem[];
}

interface Props {
  items?: MenuItem[];
  variant?: 'navbar' | 'sidebar';
}

const menuItems: MenuItem[] = [
  {text: '首页', href: '/'},
  {text: '关于我们', href: '/about'},
  {
    text: '产品',
    href: '/products',
    children: [
      {text: '产品1', href: '/products/1'},
      {text: '产品2', href: '/products/2'},
    ],
  },
  {text: '联系我们', href: '/contact'},
];

const currentPath = Astro.url.pathname;
const isActive = (href: string, hasChildren = false) => {
  if (href === '#') {
    return false;
  }

  // 处理父级菜单的高亮逻辑
  if (hasChildren) {
    // 如果当前路径完全匹配，直接返回true
    if (currentPath === href) {
      return true;
    }
    // 提取前缀进行比较
    const urlParts = href.split('/').filter(Boolean);
    const currentParts = currentPath.split('/').filter(Boolean);

    // 如果href有前缀部分，检查当前路径是否匹配这个前缀
    if (urlParts.length > 0 && currentParts.length > 0) {
      return urlParts[0] === currentParts[0];
    }
    // 检查是否是子路径
    return currentPath.includes(href + '/');
  }

  // 子菜单项也使用前缀匹配
  const urlParts = href.split('/').filter(Boolean);
  const currentParts = currentPath.split('/').filter(Boolean);

  if (urlParts.length > 0 && currentParts.length > 0) {
    return urlParts[0] === currentParts[0];
  }

  return currentPath === href;
};

const SIDEBAR_CLASSES = {
  link: 'block py-2 px-3 text-sm text-gray-700 hover:text-gray-900 hover:bg-gray-50 rounded-md',
  activeLink: 'text-blue-500 bg-blue-50',
  group: 'mb-4',
  groupTitle:
    'sidebar-group-title flex items-center justify-between font-medium text-gray-900 mb-2 p-2 cursor-pointer hover:text-blue-500 rounded-md hover:bg-gray-50',
  groupContent: 'ml-2 space-y-1 transition-all duration-200 ease-in-out',
  icon: 'w-4 h-4 transition-transform duration-200',
} as const;

const {items = menuItems, variant = 'navbar'} = Astro.props;
---

{
  variant === 'sidebar' ? (
    <nav class="space-y-1">
      {
        items.map((item, index) => (
          <div class={SIDEBAR_CLASSES.group}>
            {
              item.children ? (
                <div data-group-id={index}>
                  <div
                    class:list={[
                      SIDEBAR_CLASSES.groupTitle,
                      {[SIDEBAR_CLASSES.activeLink]: isActive(item.href, true)},
                    ]}
                  >
                    <span>{item.text}</span>
                    <svg
                      class={SIDEBAR_CLASSES.icon}
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M19 9l-7 7-7-7"
                      />
                    </svg>
                  </div>
                  <div class={`${SIDEBAR_CLASSES.groupContent} hidden`} data-content>
                    {
                      item.children.map(child => (
                        <a
                          href={child.href}
                          class:list={[
                            SIDEBAR_CLASSES.link,
                            {[SIDEBAR_CLASSES.activeLink]: isActive(child.href)},
                          ]}
                        >
                          {child.text}
                        </a>
                      ))
                    }
                  </div>
                </div>
              ) : (
                <a
                  href={item.href}
                  class:list={[
                    SIDEBAR_CLASSES.link,
                    {[SIDEBAR_CLASSES.activeLink]: isActive(item.href)},
                  ]}
                >
                  {item.text}
                </a>
              )
            }
          </div>
        ))
      }
    </nav>
  ) : (
    <nav class="bg-white shadow-lg">
      <div class="max-w-6xl mx-auto px-4">
        <div class="flex justify-between">
          <!-- Logo -->
          <div class="flex space-x-4">
            <a href="/" class="flex items-center py-5 px-2 text-gray-700 hover:text-gray-900">
              <svg
                class="h-6 w-6 mr-1 text-blue-500"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"
                />
              </svg>
              <span class="font-bold">网站名称</span>
            </a>
          </div>

          <!-- 桌面导航 -->
          <div class="hidden md:flex items-center space-x-1">
            {
              items.map(item => (
            item.children ? (
                <div class="relative group">
                  <button
                    class:list={[
                      'py-5 px-3 flex items-center',
                      isActive(item.href, true) ? 'text-blue-500' : 'text-gray-700 hover:text-gray-900',
                    ]}
                  >
                    {item.text}
                    <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                    </svg>
                  </button>
                  <div class="absolute hidden group-hover:block bg-white shadow-lg rounded-md mt-0 w-48">
                    {
                      item.children.map(child => (
                        <a
                          href={child.href}
                          class:list={[
                            'block px-4 py-2 text-gray-700 hover:bg-gray-100',
                            {'bg-gray-100': isActive(child.href)},
                          ]}
                        >
                          {child.text}
                        </a>
                      ))
                    }
                  </div>
                </div>
              ) : (
                <a
                  href={item.href}
                  class:list={[
                    'py-5 px-3',
                    isActive(item.href)
                      ? 'text-blue-500 border-b-2 border-blue-500'
                      : 'text-gray-700 hover:text-gray-900',
                  ]}
                >
                  {item.text}
                </a>
              )
          ))
            }
          </div>

          <!-- 移动端菜单按钮 -->
          <button id="menuButton" class="md:hidden p-2 focus:outline-none">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>

        <!-- 移动端菜单 -->
        <div id="mobileMenu" class="hidden md:hidden pb-4">
          {
            items.map(item => (
          item.children ? (
              <div class="mobile-dropdown">
                <button class="w-full text-left px-3 py-2 flex justify-between items-center">
                  {item.text}
                  <svg
                    class="w-4 h-4 transform transition-transform duration-200"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                  </svg>
                </button>
                <div class="hidden pl-4">
                  {
                    item.children.map(child => (
                      <a
                        href={child.href}
                        class:list={[
                          'block px-3 py-2',
                          isActive(child.href) ? 'text-blue-500' : 'text-gray-700 hover:text-gray-900',
                        ]}
                      >
                        {child.text}
                      </a>
                    ))
                  }
                </div>
              </div>
            ) : (
              <a
                href={item.href}
                class:list={[
                  'block px-3 py-2',
                  isActive(item.href) ? 'text-blue-500' : 'text-gray-700 hover:text-gray-900',
                ]}
              >
                {item.text}
              </a>
            )
        ))
          }
        </div>
      </div>
    </nav>
  )
}

<script define:vars={{SIDEBAR_CLASSES}}>
// 保持引用以避免重复查询 DOM
let initialized = false;

function initializeSidebar() {
  if (initialized) {
    return;
  }

  // 处理所有侧边栏组
  document.querySelectorAll('[data-group-id]').forEach(group => {
    const title = group.querySelector('.sidebar-group-title'); // 使用简单的类名选择器
    const content = group.querySelector('[data-content]');
    const icon = title?.querySelector('svg');

    if (!content || !icon || !title) {
      return;
    }

    title.addEventListener('click', () => {
      content.classList.toggle('hidden');
      icon.classList.toggle('rotate-180');
    });

    // 检查是否需要展开当前项
    const hasActiveChild = content.querySelector(`.${SIDEBAR_CLASSES.activeLink}`);
    if (hasActiveChild) {
      content.classList.remove('hidden');
      icon.classList.add('rotate-180');
    }
  });

  initialized = true;
}

// 确保 DOM 完全加载后初始化
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', initializeSidebar);
}
else {
  initializeSidebar();
}

// 移动端菜单切换
const menuButton = document.getElementById('menuButton');
const mobileMenu = document.getElementById('mobileMenu');

menuButton?.addEventListener('click', () => {
  mobileMenu?.classList.toggle('hidden');
});

// 移动端下拉菜单
document.querySelectorAll('.mobile-dropdown button').forEach(button => {
  button.addEventListener('click', () => {
    const content = button.nextElementSibling;
    const icon = button.querySelector('svg');

    content?.classList.toggle('hidden');
    icon?.classList.toggle('rotate-180');
  });
});
</script>

<style>
.mobile-dropdown button:focus svg {
  transform: rotate(180deg);
}

[data-content] {
  transition: all 0.3s ease-in-out;
}
</style>
